# Google Gemini API Key (required unless using Vertex AI)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Use Vertex AI instead of Gemini API
# GOOGLE_GENAI_USE_VERTEXAI=TRUE

# Optional: Custom model (defaults to gemini-2.5-flash)
# LITELLM_MODEL=gemini-2.5-flash

# Server configuration
HOST=localhost
PORT=10002
